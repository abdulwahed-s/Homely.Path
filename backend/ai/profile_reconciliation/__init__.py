"""Profile Reconciliation Agent package (AI Developer 1).

Reports cross-/intra-document conflicts for user confirmation. It never decides
which value is correct and never computes income or readiness. Detectors:
per-document gross-total, plus cross-document pay-frequency, gross-pay,
duplicate pay-period, and person-name checks.
"""
