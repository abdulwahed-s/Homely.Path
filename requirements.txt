# Core models and validation
pydantic>=2.7,<3
jsonschema>=4.22,<5
python-dotenv>=1.0,<2
pytest>=8.2,<9
pytest-asyncio>=0.23,<1
PyMuPDF>=1.24            # PDF text extraction, rasterization, page rendering
openai>=1.40             # vision model (classification + extraction); needs OPENAI_API_KEY
Pillow>=10               # image handling
rapidocr-onnxruntime>=1.2.3,<1.3 # OCR word boxes for rasterized (image-only) PDFs
fastapi>=0.110           # HTTP endpoints (all five /internal/ai/* routes); includes CORSMiddleware
uvicorn>=0.29            # ASGI server (python serve.py)
python-multipart>=0.0.9  # multipart/form-data file uploads on /extract
firebase-admin>=6.5       # verify Firebase ID tokens on /internal/ai/* (Authorization: Bearer)
